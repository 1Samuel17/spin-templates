[package]
name = "api-explorer"
authors = ["{{authors}}"]
description = "API Explorer UI — Swagger-style endpoint tester for local development"
version = "0.1.0"
rust-version = "1.78"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
anyhow = "1"
spin-sdk = "5.2.0"
